# 复制本文件为 .env 并填入真实值（.env 已被 .gitignore 排除，禁止提交真实密钥）

# DeepSeek API Key（可选）：仅作为首次启动时的默认模型种子；之后在前端“模型设置”页管理模型
DEEPSEEK_API_KEY=your-deepseek-api-key-here

# 允许访问的前端来源，逗号分隔
ALLOWED_ORIGINS=http://localhost:5173

# Qdrant 连接地址（Phase 4 使用；本地原生运行默认 http://localhost:6333）
QDRANT_URL=http://localhost:6333

# HuggingFace 镜像（可选；国内网络下载 BGE-M3 模型更快）
# HF_ENDPOINT=https://hf-mirror.com
